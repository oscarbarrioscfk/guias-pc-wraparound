import { defineConfig } from "astro/config";

// Hosting (Netlify / Vercel / Cloudflare Pages / GitHub Pages) is still open —
// see docs/plataforma.md. No `site`/`base` pinned yet on purpose.
export default defineConfig({});
